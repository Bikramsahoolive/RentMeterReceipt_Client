import { LandlordRequestPipe } from './landlord-request.pipe';

describe('LandlordRequestPipe', () => {
  it('create an instance', () => {
    const pipe = new LandlordRequestPipe();
    expect(pipe).toBeTruthy();
  });
});
